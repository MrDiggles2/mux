{
  "name": "mux",
  "version": "0.0.2",
  "bin": "./main.js",
  "scripts": {
    "start": "DEBUG=1 MUX_DEV_MODE=true ts-node src/main.ts start",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean && tsc --project tsconfig.build.json && cp package.json LICENSE dist",
    "build": "cd dist && npm pack --pack-destination=../"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.0",
    "tail": "^2.2.4",
    "terminal-kit": "^3.0.0",
    "tree-kill": "^1.2.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "devDependencies": {
    "@types/tail": "^2.2.1",
    "@types/terminal-kit": "^2.5.0"
  }
}
